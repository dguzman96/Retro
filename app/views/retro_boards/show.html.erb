<p id="notice"><%= notice %></p>
<h2 class="retro_board_title"><%= @retro_board.title %></h2>
<p class="retro_board_sub_title"><%= @retro_board.description %></p>
<div id="retro_items_wrapper">
 <%= render @retro_board.retro_items %>
 <div id="form">
  <%= render "retro_items/form" %>
 </div>
</div>
<div class="links">
 <%= link_to 'Edit', edit_retro_board_path(@retro_board) %> |
 <%= link_to 'Delete', retro_board_path(@retro_board), method: :delete, data: { confirm: "Are you sure?" } %> |
 <%= link_to 'Back', retro_boards_path %>
</div>
