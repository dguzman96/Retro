<%= form_with(model: retro_board, local: true) do |form| %>
  <% if retro_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(retro_board.errors.count, "error") %> prohibited this retro_board from being saved:</h2>

      <ul>
        <% retro_board.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
